package:
  name: intake-ids
  version: 0.0.1

source:
  path: ..

build:
  number: 0
  script:
    - python setup.py install --single-version-externally-managed --record=record.txt
  noarch: python

requirements:
  run:
    - intake
    - pandas
    - requests
    - pydantic
    - isodate

about:
  home: url
  license: MIT
  license_file: LICENSE
  summary: IDS plugin